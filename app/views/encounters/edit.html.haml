%h1 Editing encounter

= render 'form'

%p
  %h2 Charcters in campaign
  = render :partial => "characters/list", :locals => {:characters => @campaign_characters}
%p
  %h2 Charcters in encounter
  = form_for [@campaign, @encounter], :url => init_campaign_encounter_path(@campaign, @encounter), :html => {:method => 'post', :class => "characters_list"} do |f|
    %table#initialization
      %tr
        %th Name
        %th Base Initiative
        %th Initiative Roll

      - @characters.each do |character|
        %tr.character
          %td.name= character.name
          %td.base_initiative= character.base_initiative
          %td
            %input{:id => "characters_#{character.id}_roll", :name => "characters[#{character.id}][roll]"}

= link_to 'Show', @encounter
\|
= link_to 'Back', campaign_encounters_path(@campaign)
