%h1= "Running #{@encounter.name}"
%p
  %b Name:
  = @encounter.name

  %table
    - @characters.each do |name, init|
      %tr.character
        %td.name= name
        %td.init= init

  %button#previous previous round
  %button#next next round
